gouldnetworksbianca
btAJx5V#3

HTML:
added span #current-day-time and removed initial day time

Added the following id's to divs to be able to change them dynamically:
#day1-name
#day2-name
#day3-name
#day4-name
#day5-name

#day1-degrees
#day2-degrees
#day3-degrees
#day4-degrees
#day5-degrees

#day1-icon
#day2-icon
#day3-icon
#day4-icon
#day5-icon

JS:
Removed:
// let hours = now.getHours();
// let minutes = now.getMinutes();

// let days = [
//   "Sunday",
//   "Monday",
//   "Tuesday",
//   "Wednesday",
//   "Thursday",
//   "Friday",
//   "Saturday"
// ];
// let day = days[now.getDay()];
Replaced with the built in javascript function to get all of that information.
now.toLocaleString('en-US', { weekday: 'long', hour: 'numeric', minute: 'numeric', hour12: true })
https://caniuse.com/?search=toLocaleString <--Shows what browsers a javascript function works on.

Also put the day time into a function so you can add setTimeout to make the function run every X amount of milliseconds.
function outDayTime(){
  const now = new Date();
  const currentDayTime = document.querySelector("#current-day-time");
  currentDayTime.innerHTML = now.toLocaleString('en-US', { weekday: 'long', hour: 'numeric', minute: 'numeric', hour12: true });
  setTimeout(outDayTime, 10000);
}
outDayTime();

Removed:
// function search(event) {
//   event.preventDefault();
//   let apiEndpoint = "https://api.openweathermap.org/data/2.5/weather?q=";
//   let apiKey = "6878b5f626b7c98659b0400085d3bba8";
//   let city = document.querySelector("#search-city").value;
//   let units = "imperial";
//   let apiUrl = `${apiEndpoint}${city}&appid=${apiKey}&units=${units}`;
//   axios.get(apiUrl).then(displayTemp);
// }
// let form = document.querySelector("#search-form");
// form.addEventListener("submit", search);


function getWeatherAPI(cityName){
  const apiEndpoint = "https://api.openweathermap.org/data/2.5/weather?q=";
  const apiKey = "6878b5f626b7c98659b0400085d3bba8";
  const units = "imperial";
  const apiUrl = `${apiEndpoint}${cityName}&appid=${apiKey}&units=${units}`;
  axios.get(apiUrl).then(displayTemp);
}

function search(event) {
  event.preventDefault();
  const city = document.querySelector("#search-city").value;
  getWeatherAPI(city);
}
let form = document.querySelector("#search-form");
form.addEventListener("submit", search);
Split the search function in to 2 parts. First part search() just gets called on search submission, grabs the city name and then calls the 
second part getWeatherAPI. The second part takes a city as a variable and calls the API like you had before. This allows you to easily run 
the API without using the search.

CSS:

